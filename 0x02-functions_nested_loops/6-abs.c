#include "main.h"
/**
 * _abs - function to print the absolute values
 * @c: argument to be pass in the function
 * Return: 0
 */
int _abs(int c)
{
if (c > 0 || c == 0)
{
return (c);
}
else
return (c * -1);
}
