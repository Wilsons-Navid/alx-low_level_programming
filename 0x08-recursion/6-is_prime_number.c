#include <stdio.h>
#include "main.h"

/**
 * is_prime_number - function to print prime number
 * @n: interger
 * Return: 0 if not prime and 1 if otherwise
 */

int is_prime_number(int n)
{
if (n % 2 == 1 || n % 2 == -1)
return (0);
else
{	
return (1);
}
}
