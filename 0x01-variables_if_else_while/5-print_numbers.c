#include <stdio.h>

/**
 * main - program that prints all singledigits in base 10
 * all your code should be in the main function
 * Return:0
 */

int main(void)
{
int n;
while (n<10)
{
printf("%d", n);
n += 1;
}
printf("\n");
return (0);
}
